name: Publish Package

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
    source_branch_validation:
      name: Source branch validation
      runs-on: ubuntu-latest
      if: github.event_name == 'pull_request' && github.base_ref == 'main'
      steps:
        - name: Validate source branch
          run: |
            SOURCE_BRANCH="${{ github.head_ref }}"
            if echo "$SOURCE_BRANCH" | grep -Eq '^(develop|bugfix/.+|hotfix/.+|fix/.+|release/.+)$'; then
              echo "Source branch '$SOURCE_BRANCH' is allowed."
            else
              echo "Source branch '$SOURCE_BRANCH' is NOT allowed."
              exit 1
            fi
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm ci

      - run: npm test

  tag_validation:
    name: Tag validation
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.base_ref == 'main'
    steps:
      - uses: actions/checkout@v4

      - name: Get version from package.json
        id: version
        run: echo VERSION=$(cat package.json | grep '"version"' | head -1 | awk '{print $2}' | sed 's/"//g' | sed 's/,//g') >> $GITHUB_OUTPUT

      - name: Check tag exists
        run: |
          if git ls-remote --tags origin "refs/tags/v${{ steps.version.outputs.VERSION }}" | grep -q "refs/tags/v${{ steps.version.outputs.VERSION }}"; then
            echo "Tag v${{ steps.version.outputs.VERSION }} already exists."
            exit 1
          else
            echo "Tag v${{ steps.version.outputs.VERSION }} does not exist."
          fi

  publish:
    name: Publish to npm and Create GitHub Release
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org

      - run: npm ci

      - name: Get version from package.json
        id: version
        run: echo VERSION=$(cat package.json | grep '"version"' | head -1 | awk '{print $2}' | sed 's/"//g' | sed 's/,//g') >> $GITHUB_OUTPUT

      - name: Tag validation
        id: tag_validation
        run: |
          if git ls-remote --tags origin "refs/tags/v${{ steps.version.outputs.VERSION }}" | grep -q "refs/tags/v${{ steps.version.outputs.VERSION }}"; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Setup git
        if: steps.tag_validation.outputs.exists == 'false'
        run: |
          git config user.email "github-actions@github.com"
          git config user.name "GitHub Actions"

      - name: Create and push tag
        if: steps.tag_validation.outputs.exists == 'false'
        run: |
          git tag v${{ steps.version.outputs.VERSION }}
          git push origin v${{ steps.version.outputs.VERSION }}

      - name: Publish to npm
        if: steps.tag_validation.outputs.exists == 'false'
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        if: success() && steps.tag_validation.outputs.exists == 'false'
        with:
          tag_name: v${{ steps.version.outputs.VERSION }}
          body: Published v${{ steps.version.outputs.VERSION }} to npm
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
